# Локальная демонстрация без TLS: Hub напрямую на 8080.
# Только для стенда разработчика; в проде и на пилоте TLS обязателен.
services:
  hub:
    ports:
      - "8080:8000"
    environment:
      HUB_PUBLIC_URL: http://localhost:8080
