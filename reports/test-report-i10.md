# Отчёт test-agent — итерация I-10 (витрина в стиле Claude, ревизия спеки 1.10)

Дата: 2026-08-27. Репозиторий: `~/Documents/opencode`, ветка `corp/i4-sso-connectors`.
Спека: `corp/docs/spec.md` ревизии 1.10 (`d466fa0b5d`, `77acf688b2`).
Критерии: `corp/docs/acceptance-criteria.yaml`, AC-200…AC-239.

## 1. Покрытие критериев — 40 из 40

Каждый критерий помечен идентификатором в имени теста (`test("AC-NNN: …")`) — по соседнему
корп-соглашению. Проверено машинно: из AC-200…AC-239 нет ни одного без собственного теста.

| Критерий | Где проверяется |
|---|---|
| AC-200, AC-201, AC-202, AC-203 | `packages/opencode/src/corp/permission-groups.test.ts` |
| AC-204, AC-205, AC-214 | `packages/app/src/corp/connector-page.test.ts`, `packages/tui/src/corp/permissions-view.test.ts` |
| AC-206, AC-207 | `permissions-route.test.ts`, `connectors.test.ts`, `connector-page.test.ts`, `permissions-view.test.ts` |
| AC-208…AC-211, AC-213, AC-237, AC-238 | `packages/opencode/src/corp/permissions.test.ts` (S-Q13) |
| AC-212 | `connector-page.test.ts`, `permissions-view.test.ts`, `permissions-route.test.ts` |
| AC-215, AC-216 | `permission-groups.test.ts`, `packages/tui/src/corp/connectors-view.test.ts` |
| AC-217, AC-218, AC-219 | `packages/app/src/corp/connectors-view.test.ts`, `packages/tui/src/corp/connectors-view.test.ts` |
| AC-220, AC-221, AC-222 | `packages/opencode/src/corp/connectors.test.ts` (исполняется тело мемо витрины), `packages/tui/src/corp/connectors-view.test.ts` |
| AC-223, AC-224 | `packages/app/src/corp/connectors-view.test.ts`, `packages/tui/src/corp/connectors-view.test.ts` |
| AC-225, AC-226 | `packages/app/src/corp/connectors-view.test.ts` (каскад CSS в happy-dom) + `packages/app/test-layout/connectors-layout.layout.test.ts` (**измеренная геометрия в браузере**) |
| AC-227, AC-228, AC-229, AC-230 | `packages/app/src/corp/connectors-view.test.ts`, `packages/tui/src/corp/connectors-view.test.ts`; AC-230 сверх того — измерением (`test-layout`) |
| AC-231, AC-234 | `packages/app/src/corp/connectors-view.test.ts`, `packages/app/src/corp/dictionary.test.ts` |
| AC-232, AC-235, AC-236 | `packages/app/src/corp/tokens.test.ts` (S-Q12) |
| AC-233 | `packages/app/src/corp/dictionary.test.ts` (+ кросс-проверка словаря TUI) |
| AC-239 | `packages/opencode/src/corp/permissions-route.test.ts` |

### Критически важные проверки — как именно они сделаны

**S-Q13 / правило порядка ключей (AC-209, AC-237, AC-238).**
`packages/opencode/src/corp/permissions.test.ts` не сравнивает состав ключей: конфиг **получается
сохранением** — двумя патчами `permissionGroupsPatch`, применёнными тем же механизмом, каким их
применяет `Config.updateGlobal` к `.jsonc` (`jsonc-parser` `modify`/`applyEdits`, F21), — и дальше
прогоняется через `Permission.evaluate`. Проверены оба направления: `rest=allow` + группа `deny` даёт
инструменту `deny`, `rest=deny` + группа `allow` даёт `allow`; инструмент вне групп получает режим
`rest`; ключи `edit`/`bash`/`*` и ключи другого коннектора сохраняют **прежнее действие** (сравнение
с `Permission.evaluate` по конфигу ДО сохранения, а не с константой в тесте).

**Инъекция «wildcard последним» ловится.** Отдельный `describe` строит из полученного конфига тот же
блок с wildcard в конце и доказывает три вещи: состав ключей у правильной и у испорченной записи
**идентичен** (`Object.entries(...).sort()` равны), порядок различается, а `Permission.evaluate` даёт
противоположный ответ (`deny` против `allow`). Тот же приём для AC-238: с испорченным порядком
запрещённая группа снова попадает в список, который видит модель (`Permission.disabled` — тот же
механизм, которым список собирает `LLMRequestPrep.resolveTools`).

**S-Q12 / разрешимость CSS-токенов (AC-235, AC-236, AC-232).**
`packages/app/src/corp/tokens.test.ts`: настоящие `packages/ui/src/styles/theme.css`,
`colors.css`, `v2/styles/theme.css`, `v2/styles/colors.css` и CSS десяти компонентов корп-окон
грузятся в happy-dom, корп-окно монтируется там же, значения читаются `getComputedStyle`. Список
переменных **собирается из файлов** (полное выражение `var(--x)` или `var(--x, запас)` — запас
сохраняется, потому что он часть объявления), а не выписан в тест: тест это и утверждает — семь
переменных `SegmentedControlV2` попадают в список ровно потому, что встречаются в его файле.
Обе темы обходятся тем способом, каким их различает сама тема: тёмная — раскрытием
`@media (prefers-color-scheme: dark)` темы `ui` и атрибутом `data-color-scheme="dark"` темы `v2`;
отдельный тест утверждает, что светлая и тёмная **действительно различаются** (иначе тёмная ветка
была бы вхолостую). Приёмочное свойство AC-236 проверяется делом: тот же прогон по тексту
`dialog-shell.css` с вырезанным блоком алиасов обязан назвать каждую из семи переменных.

Оговорка по методу, которую стоит знать при чтении файла: happy-dom не разбирает `@layer` и ломается
на `@media` внутри правила, а `color: var(--x)` отбрасывает значения, не похожие на цвет. Поэтому
проба ставится через собственную переменную (`--probe: var(--x)`) и читается обратно, а тексты тем
нормализуются разворачиванием слоя. Ни одно объявление при этом не переписывается. Переменные,
объявленные в том же файле, где и использованы (`--menu-v2-accent`, `--bottom-fade`), считаются
внутренними переменными компонента и в проверку темы не входят — иначе правило пришлось бы подпирать
списком исключений, а корп-алиасы (объявлены в `dialog-shell.css`, использованы в
`segmented-control-v2.css`) как раз кросс-файловые и проверяются.

**`permission_state`, все режимы и `mixed` (AC-213).** Конфиги для свёртки тоже получены сохранением:
словарь из трёх однострочных групп сохраняется с нужными режимами, а свёртка считается по словарю с
одной группой из тех же трёх инструментов — так `allow/deny/ask/mixed` получаются наблюдаемо, а не
подстановкой. Отдельно проверено, что режим берётся из `Permission.evaluate`, а не чтением ключа:
инструменты без собственных ключей наследуют режим wildcard, а правило, стоящее в конфиге **после**
блока alias, переводит группу в `mixed`.

**Деградация `permission_groups` (AC-202, AC-203).** Битые группы поодиночке (`default` вне
перечисления, пустой `title`, пустой или нестроковый `tools`, повторный `id`) — выбрасывается только
такая группа, её инструменты уходят в `rest`, число выброшенных доезжает наружу; весь блок целиком
(нарушенный конверт в девяти формах) и `version > 1` — карточка не отбрасывается, экран строится по
`permission_model`, а дословное значение уцелело в карточке и в кэше (проверено чтением файла кэша).

**Шесть пустых состояний (AC-220…AC-222).** Тело мемо `empty` витрины исполняется как есть (тот же
приём, что в `dialog-actions.test.ts`): проверены текст **и** предлагаемое действие каждого из шести
состояний, их попарная различимость, и что на вкладке «Все» пустого состояния нет — там причина в
поиске. То же в TUI.

**Контраст (AC-231).** Запрещённые классы проверяются по всему `components/corp/**`, а не по списку
файлов: `text-11-regular` не встречается ни разу (и его нет в `utilities.css`), `text-text-weaker` не
встречается ни разу, каждый размерный класс корп-экранов объявлен в `utilities.css`.

### Дополнение: микроревизия 1.11.1 — AC-273…AC-278 (6 из 6)

Пока шла работа, в ветку легла микроревизия спеки 1.11.1 (`53ba602998`, `6e049bf08a`, `c4f86964aa`)
и её реализация (`910d51d059`, `db8cc25750`, `961259b4b8`, `e88d69fae2`). Покрыты все шесть новых
критериев.

| Критерий | Где проверяется |
|---|---|
| AC-273 | `packages/app/src/corp/no-hub-texts.test.ts`, `packages/tui/src/corp/no-hub-texts.test.ts` |
| AC-274 | `packages/app/src/corp/connector-page.test.ts`, `packages/tui/src/corp/permissions-view.test.ts` |
| AC-275 | `packages/app/src/corp/no-hub-texts.test.ts`, `packages/tui/src/corp/no-hub-texts.test.ts` |
| AC-276, AC-277 | `packages/app/src/corp/tokens.test.ts` |
| AC-278 | `packages/app/src/corp/no-hub-texts.test.ts`, `packages/tui/src/corp/no-hub-texts.test.ts` |

**AC-275 — матрица по отрисованному тексту.** Самая ценная из шести и сделана буквально по
формулировке. Матрица строится в обеих оболочках и в обоих языках; **ключ каждой ветви выбирает сам
код**: тела мемо `empty`, `banner`, `unavailableSource`/`sourceDown` и функций `statusKey`,
`connectErrorKey`, `permissionsUnavailableKey` берутся из исходников и исполняются как есть, а текст
получается подстановкой `{{…}}` в настоящий словарь. Ветви: шесть пустых состояний, баннер над
непустым списком (свежая ошибка и данные из кэша), предупреждение о неполном каталоге, все состояния
и статусы карточки, оба способа подключения, пять видов модели прав (включая неразобранную и
Hub-зависимую), экран входа, подтверждение «Убрать из списка», четыре класса ошибок подключения —
свыше 30 ветвей на язык. Проверка: ни в одной нет подстроки «hub» без учёта регистра.

Полнота матрицы утверждена отдельно и машинно: перечни состояний, статусов и способов подключения
читаются из объявлений `CardState`/`CardStatus`/`ServerMode` в `packages/opencode/src/corp/schema.ts`,
классы ошибок — из `ERROR_CLASSES` в `corp/errors.ts`, и таблицы `STATE_KEY`/`STATUS_KEY` витрины
сверяются с ними ключ в ключ. Новое значение перечисления без новой ветви матрицы валит тест.
Отдельный тест утверждает обратную сторону: те же ветви в сборке **с** Hub показывают прежние тексты
(состояние 3 — «Hub недоступен», заблокированный экран прав — прежний ключ ревизии 1.7, facade
подключается как прежде), а слово «Hub» в словаре законно и тестом не запрещается — запрещено только
быть достижимым в сборке без Hub.

**AC-276/AC-277 — граница алиаса.** В корп-окне `--surface-disabled` разрешается в обеих темах и
значение приходит из темы (в светлой и тёмной оно разное); вне корп-окна та же переменная и те же
семь прежних алиасов не разрешаются; на `:root` алиас не объявлен ни в корп-CSS, ни в темах. Область
AC-235 при этом не сужена — переменная осталась в общем наборе токенов, по которому идёт основная
проверка.

## 2. Прогоны

Итоговые прогоны (после того, как в ветку легла реализация микроревизии 1.11.1):

| Пакет | Команда | Результат |
|---|---|---|
| `packages/opencode` (`src/corp` целиком) | `bun test src/corp` | **457 pass, 0 fail**, 2271 утверждение, 17 файлов |
| `packages/opencode` (наборы `corp/`) | `bun test ../../corp/` | **92 pass, 0 fail**, 5 файлов — реестр правок upstream снова совпадает с деревом |
| `packages/app` (unit) | `bun test --preload ./happydom.ts ./src` | **541 pass, 0 fail**, 2478 утверждений, 77 файлов |
| `packages/app` (раскладка, Playwright) | `bun x playwright test --config test-layout/playwright.config.ts` | **21 passed**, Chromium (канал `chrome`) |
| `packages/tui` | `bun test --timeout 30000` | **265 pass, 1 skip, 8 fail**, 1481 утверждение, 50 файлов |

Типизация: `bun run typecheck` в `packages/opencode`, `packages/app` и `packages/tui` — код 0 во всех
трёх.

**Разбор падений.**

- `packages/tui` — те же **8 базовых** падений, что были до начала работы (`tui sync` ×3 и
  hydration ×5). Ни одно не корпоративное, ни одно не изменилось; снято до правок и после.
- **Корп-падений нет ни в одном пакете.** По ходу работы их было два: `AC-235` в светлой и тёмной
  темах — найденный дефект `BUG-I10-001` (`--surface-disabled`). Дефект закрыт коммитом `910d51d059`
  (алиас в корп-CSS по AC-276), падавший тест остался в сьюте регрессионным и зелен.
- Корп-падений, вызванных ревизией тестов, нет: восемь ожидаемо упавших тестов приведены к новой
  вёрстке и зелены; ещё три пришлось ревизовать по ходу под микроревизию 1.11.1 (см. ниже).

## 3. Ревизия ранее принятых тестов

Ожидалось семь падений; фактически их было **восемь** — к шести в `connectors-view.test.ts` и одному
в `dictionary.test.ts` добавился модульный сбой `packages/opencode/src/corp/connectors.test.ts`
(файл падал целиком на верхнем уровне). Ни одно утверждение не ослаблено — ниже, что именно сделано.

| Тест | Было | Стало |
|---|---|---|
| `connectors-view.test.ts` AC-177 «бейдж — тот же элемент, что «устаревший»» | бейдж на витрине | признак в колонке «Статус»: текст **и** цветовая точка, точка `aria-hidden`, цвет — семантический токен темы; бейдж проверяется там, где колонки нет, — на странице коннектора (AC-224), и отдельно утверждается, что на витрине его больше нет |
| `connectors-view.test.ts` AC-177 «подпись статуса сохраняется… `text-11-regular text-text-weaker`» | требовалось **наличие** пары классов | требование **перевёрнуто в запрет** по S-D12: этой пары нет ни разу во всём `components/corp/**`, `text-11-regular` не объявлен в `utilities.css`, каждый размерный класс корп-экранов объявлен. Это ревизия, а не ослабление: смысл AC-177 («признак не выражается мелкой подписью») сохранён и усилен переездом признака в колонку |
| `connectors-view.test.ts` AC-177 «нет кнопки «Подключить»» | кнопки на витрине | кнопки на странице коннектора (S-D11) + утверждение, что витрина их не дублирует |
| `connectors-view.test.ts` AC-172 «Отключить» | витрина | страница коннектора, та же проверка «ровно одно вхождение и ближайший `Show` — про действие `disconnect`» |
| `connectors-view.test.ts` AC-172/S-V19 explain | витрина | `STATE_KEY` остался на витрине, `explain()` проверяется на странице; «кнопки-совета» нет ни там, ни там |
| `connectors-view.test.ts` S-V17 подтверждение | витрина | `DialogForgetConnector` на странице коннектора, проверки те же |
| `dictionary.test.ts` AC-166 | список из одного файла витрины | список расширен до **всего** `components/corp/**`; требование усилено: текст ключа не встречается **ни в одном** корп-компоненте. Туда же расширен AC-98 (запрет кириллицы), с утверждением, что каталог непуст |
| `connectors.test.ts` (opencode) | `presetsFor`/`toolsFor` из витрины, `emptyMessage` одной строкой | `presetsFor`/`toolsFor` берутся из `dialog-connector.tsx`, `empty` — из витрины с её нынешними аргументами и возвращает пару «текст + действие»; блок расширен до шести пустых состояний |

Из ранее принятых тестов ничего не удалено и не ослаблено. Механизм `dialog-actions.test.ts`
(вырезание `allows()` из исходника витрины TUI) не тронут — файл проходит как проходил.

**Ревизия по ходу под микроревизию 1.11.1.** Реализация 1.11.1 развела причину «экран прав
заблокирован» на два текста по признаку «адрес Hub задан» и добавила `ConnectorPresets` обязательный
проп `hubConfigured`. Три моих проверки пришлось привести к этому — тоже без ослабления: вместо
литерала ключа проверяется, что ключ выбирает `permissionsUnavailableKey(hubConfigured)`, и что этот
выбор даёт оба текста, а причина называется всегда (`connector-page.test.ts` AC-206/AC-207,
`connectors.test.ts` AC-156/AC-207). Прежняя развилка «словарь групп либо пресеты» проверяется на
том же месте — теперь внутри внешней ветви «Hub в сборке нет», и порядок вложенности утверждается
явно.

## 4. Диспуты — все три закрыты

Резолюции лежат рядом с диспутами, коммит `0714fd7cbb`.

**`dispute-I10-02-permission-groups-route` — в пользу dev-agent, полностью.** Спека главнее промпта:
S-V1 ревизии 1.10 и AC-239 фиксируют расширение существующего `PUT /corp/connectors/:alias/permissions`
телом `{modes}`, отдельного пути и поля `all` в спецификации нет. Реализация проверена и соответствует.
Правки спеки не требуется. Резолюция: `corp/disputes/dispute-I10-02-permission-groups-route.resolution.json`.

**`dispute-I10-04-permissions-cancel` — немедленное применение верно, AC-212 переформулирован.**
Состав страницы в S-D11 п.5 пары «Сохранить/Отмена» не содержит, а S-V23 прямо требует, чтобы
«Смешанно» исчезало **как только** выбран режим, — то есть отложенное применение противоречило бы
спецификации изнутри. AC-212 переформулирован по предложенной в диспуте проверяемой форме: прежнее
требование сохранено дословно («ни один вызов `Config.updateGlobal`, конфиг не изменён, запрос к Hub
не отправлен») и к нему добавлено более сильное — запись выполняется **только** выбором режима, а
запрос к Hub у этого вида не отправляется ни при каком действии экрана (D-35). Проверено в обеих
оболочках. Правка легла в `corp/docs/acceptance-criteria.yaml` (в дереве оказалась в коммите
`6e049bf08a` соседнего агента, который коммитил тот же файл целиком).

**`dispute-I10-05-tui-search-restore` — закрыт реализацией, вариант (а).** dev-agent доделал
`initialFilter` (`b9d161300c`, `cf64830a92`), строка реестра правок upstream заведена
(`61886ac52a`, `7fec2bf990`). Восстановление проверено тестом по всем трём составляющим S-T10:
вкладка, строка поиска **в самом поле ввода** и выделенная строка (по alias, а не по ссылке на
карточку). Оговорка диспута `not_implemented: «текст в самом поле ввода»` снята; уточнять S-T10
(вариант «б») не требуется.

## 5. Дефекты

**`BUG-I10-001` — `--surface-disabled` не объявлен ни в одной теме** (severity `medium`, **`fixed`**).
Найден новым тестом S-Q12. У заблокированной кнопки `variant="secondary"` в корп-окнах
(`<Button size="small" disabled={…}>` — умолчание варианта именно `secondary`) `background-color`
не задаётся вовсе: токена нет ни в одной теме, при том что соседние `--border-disabled` и
`--text-weak` из того же правила объявлены в обеих. Падающий тест:
`packages/app/src/corp/tokens.test.ts::AC-235` (обе темы). Ровно тот класс дефекта, ради которого
S-Q12 и вводился.

Микроревизия спеки 1.11.1 (`6e049bf08a`) завела под этот дефект **AC-276 и AC-277**: чинить алиасом
`--surface-disabled: var(--surface-weak)` в корп-`dialog-shell.css` (D-33, D-47), с требованием,
чтобы вне корп-окна переменная осталась неразрешимой. Дефект **закрыт** коммитом `910d51d059`;
падавший тест зазеленел сам и остался в сьюте регрессионным, а сверх него добавлены проверки AC-276
и AC-277 (значение приходит из темы и различается между темами; вне корп-окна не разрешается; на
`:root` алиас не объявлен; область AC-235 не сужена).

**`BUG-I10-002` — витрина показывала пустую таблицу: `TabsV2` забирал всю высоту окна**
(severity `critical`, **`fixed`**, найден заказчиком на живой сборке, исправлен `b2574261da`).
Дефект прошёл **мимо всего моего покрытия**, и это его главный урок. Разбор — в разделе 8.

Флаки не обнаружены: все прогоны повторялись и давали тот же результат, кроме восьми базовых падений
TUI, которые стабильны и не мои.

## 5а. Геометрия витрины: чего не умело покрытие и чем это закрыто

`BUG-I10-002` — рантайм-развал раскладки при 541 зелёном тесте. `TabsV2` объявляет корню
`height: 100%` (в настройках он сам себе окно), а корп-CSS давал ему `flex: none`, то есть запрет
сжиматься. В ветке прежнего `Dialog`, где высота панели определена и процент разрешается, вкладки
занимали **всю** панель: строка поиска и шапка выдавливались за нижний край окна, области прокрутки
доставалось 0 пикселей, карточки были в DOM, но невидимы. В ветке `settings-v2` высота на этом шаге
неопределённая, `height: 100%` вырождался в `auto`, и раскладка случайно оставалась целой — поэтому
дефект и не попадался на глаза.

**Почему его не поймал ни один мой тест.** Покрытие витрины состояло из двух приёмов, и ни один из
них по построению не может увидеть элемент, занявший всё окно:

- **проверки текста исходника** (`connectors-view.test.ts`, `connector-page.test.ts`,
  `dictionary.test.ts`) — они видят правильный JSX и правильные имена слотов; CSS-файл при этом тоже
  был «правильным»: `flex: none` — осмысленное объявление, и никакая строковая проверка не отличит
  его от `flex: 0 1 auto`;
- **проверки CSS-каскада в happy-dom** (`tokens.test.ts`, блок AC-184 в `connectors-view.test.ts`) —
  happy-dom **не считает раскладку**: он резолвит каскад и переменные, но не выполняет flexbox.
  Проверка AC-184 сравнивала *объявленную* высоту контейнера и вычисленные `height/min-height/overflow`
  панели — и оставалась зелёной при полностью развалившемся окне внутри этой панели.

**Чем закрыто.** `packages/app/test-layout/connectors-layout.layout.test.ts` — тест, который
**считает геометрию**. Витрина собирается вайтом теми же плагинами, что и приложение (алиас `@`,
tailwind, `vite-plugin-solid`), поднимается в настоящем Chromium и измеряется через
`getBoundingClientRect`. Всё, что определяет раскладку, — настоящее: `Dialog`/`DialogV2` в той же
обёртке Kobalte, какой их монтирует `packages/ui/src/context/dialog.tsx`, настоящие `TabsV2` и
`List`, настоящие стили приложения (`@/index.css`) и корп-`dialog-shell.css`. Своего CSS фикстура не
добавляет ни строки; ни dev-сервера, ни сервера opencode тесту не нужно — собранные скрипт и стили
вставляются в страницу.

Утверждения — ровно по `requested_test` баг-репорта:

| № | Что измеряется |
|---|---|
| 1 | каждая строка каталога ненулевой высоты; первая строка целиком внутри панели; хотя бы одна строка помещается в область прокрутки |
| 2 | порядок блоков сверху вниз строго «вкладки < поиск < шапка < область прокрутки» |
| 3 | высота вкладок ≤ 48px и меньше половины высоты панели — **это и ловит `height: 100%`** |
| 4 | область прокрутки ненулевой высоты (в пустом состоянии место намеренно отдано `corp-connectors-empty`, и ненулевую высоту обязано иметь оно) |
| 5 | левые края трёх ячеек шапки и трёх ячеек первой строки совпадают попиксельно (S-D6) |
| 6 | ни один элемент оболочки не выходит за границы панели |
| 7 | S-D10: панель растянута на всю высоту контейнера; измеренная высота одинакова при 0/1/50 карточках, на всех шести пустых состояниях и с баннером; высоты двух веток различны |
| 8 | фикстура остаётся слепком витрины: те же `data-slot`, тот же класс контейнера, те же `TabsV2 variant="pill"` и `List`, три колонки в единственной сетке корп-CSS |

Матрица — 21 сценарий: обе ветки флага × 0/1/50 карточек, шесть пустых состояний × обе ветки, баннер
с предупреждением и проверка соответствия фикстуры разметке витрины. Заодно **AC-184 и AC-230
закрыты наблюдаемо** — по измеренной высоте панели, а не по объявленной высоте контейнера.

**Фальсифицируемость доказана делом.** Фикс `b2574261da` откачен локально (правилу вкладок возвращены
`flex: none` и снят `height: auto`):

| Состояние `dialog-shell.css` | Результат набора |
|---|---|
| с фиксом | **21 passed** |
| фикс откачен | **15 failed, 6 passed** |

Первое падение называет причину дословно: «вкладки выше разумного предела в 48px, получено 510.2» —
то самое, что видел пользователь. После проверки файл восстановлен побайтово (`git status` по нему
пуст), и набор снова даёт 21 passed.

**Как гоняется.** Локально и в CI — одной и той же командой:

```
bun --cwd packages/app x playwright test --config test-layout/playwright.config.ts
```

Скрипта в `packages/app/package.json` намеренно **нет**: `package.json` — upstream-файл, и строка в
нём требует записи в реестре правок `corp/patches.md` (S-B6, S-C8). Первый заход это правило нарушил
(скрипт `test:layout` плюс строка в `.gitignore`), `corp/patches.test.ts` справедливо упал по AC-13,
и след убран коммитом `5664005cde`: запуск — командой, результаты Playwright — в
`fixture/dist/test-results`, который уже игнорируется корневым `.gitignore`. Нулевой след в реестре
— часть цены этого теста, и она уплачена.

Браузер: по умолчанию chromium из `playwright install`; если его нет, а в системе есть Google Chrome,
конфигурация переключается на канал `chrome` — скачивать ничего не нужно (локальные прогоны шли
именно так, Google Chrome 147). В `corp-ci.yml` (ubuntu-latest, браузеров Playwright там нет) нужен
отдельный шаг установки:

```yaml
      - name: Браузер для тестов раскладки
        run: bun x playwright install --with-deps chromium

      - name: Тесты раскладки корп-окон (packages/app)
        run: bun --cwd packages/app x playwright test --config test-layout/playwright.config.ts
```

Правку `.github/workflows/corp-ci.yml` вносит тот, кому это разрешено ролью: **test-agent `.github/`
не правит**. До тех пор набор гоняется локально, и его прогон фиксируется здесь. Готовый фрагмент и
подробности — `packages/app/test-layout/README.md`.

## 6. Новые и изменённые файлы тестов

Новые:
`packages/opencode/src/corp/permissions.test.ts` (S-Q13),
`packages/opencode/src/corp/permission-groups.test.ts`,
`packages/opencode/src/corp/permissions-route.test.ts`,
`packages/app/src/corp/tokens.test.ts` (S-Q12),
`packages/app/src/corp/connector-page.test.ts`,
`packages/app/src/corp/no-hub-texts.test.ts` (AC-273, AC-275, AC-278),
`packages/tui/src/corp/connectors-view.test.ts`,
`packages/tui/src/corp/permissions-view.test.ts`,
`packages/tui/src/corp/no-hub-texts.test.ts` (AC-273, AC-275, AC-278).

Новый набор измерения раскладки (BUG-I10-002):
`packages/app/test-layout/connectors-layout.layout.test.ts`,
`packages/app/test-layout/fixture/shell.tsx`,
`packages/app/test-layout/fixture/vite.config.ts`,
`packages/app/test-layout/playwright.config.ts`,
`packages/app/test-layout/tsconfig.json`,
`packages/app/test-layout/README.md`.
Ни один upstream-файл при этом не изменён: запуск — командой, а не скриптом в `package.json`,
результаты Playwright — в уже игнорируемом `fixture/dist`.

Изменённые (ревизия):
`packages/opencode/src/corp/connectors.test.ts`,
`packages/app/src/corp/connectors-view.test.ts`,
`packages/app/src/corp/dictionary.test.ts`.

## 7. Коммиты

| Хеш | Заголовок |
|---|---|
| `d2b39551bb` | тесты разрешений по группам — правило порядка через `Permission.evaluate` |
| `5fc314062c` | тесты витрины-таблицы, страницы коннектора и разрешимости CSS-токенов |
| `7846338a26` | тесты TUI ревизии 1.10 — вкладки, возврат со страницы и экран разрешений |
| `0714fd7cbb` | резолюции диспутов 10.2, 10.4 и 10.5 ревизии 1.10 |
| `f43990bd53` | ревизия тестов экрана прав под двухвариантную причину микроревизии 1.11.1 |
| `48b816e930` | тесты микроревизии 1.11.1 — двухвариантная причина и граница восьмого алиаса (AC-274, AC-276, AC-277) |
| `fcc65ee36c` | матрица отрисованных текстов сборки без Hub и тексты прежних ревизий (AC-273, AC-275, AC-278) |
| `275c86026f` | тест раскладки витрины меряет геометрию в браузере (AC-184, AC-225, AC-226, AC-230; BUG-I10-002) |
| `5664005cde` | тест раскладки не оставляет следа в upstream — запуск командой, результаты в `dist` (AC-13) |

Переформулировка AC-212 лежит в `corp/docs/acceptance-criteria.yaml`; в дереве она оказалась внутри
коммита `6e049bf08a` соседнего агента, который коммитил тот же файл целиком.

Ветка не пушилась.
